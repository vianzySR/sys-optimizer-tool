name: CI

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main]
  workflow_dispatch:
    inputs:
      environment:
        description: 'Target environment'
        default: 'staging'
        type: choice
        options:
          - staging
          - production
      skip_tests:
        description: 'Skip integration tests'
        default: false
        type: boolean

permissions:
  contents: read

jobs:
  lint:
    name: Code Quality
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
      - run: npm ci
      - run: npm run lint

  test:
    name: Integration Tests
    runs-on: ubuntu-latest
    timeout-minutes: 30
    needs: lint
    if: ${{ !inputs.skip_tests }}
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
      - run: npm ci
      - name: Run health check
        run: npm start
      - name: Verify Docker runtime
        run: |
          echo "Checking container runtime..."
          docker --version
          docker compose version
          docker ps
          echo "Runtime check: OK"

  build:
    name: Build Images
    runs-on: ubuntu-latest
    needs: test
    if: github.ref == 'refs/heads/main'
    steps:
      - uses: actions/checkout@v4
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3
      - name: Build Docker image
        run: |
          docker compose build --no-cache
          docker images | grep sys-optimizer

  deploy-staging:
    name: Deploy to Staging
    runs-on: ubuntu-24.04
    timeout-minutes: 360
    needs: build
    if: github.ref == 'refs/heads/main' || inputs.environment == 'staging'
    steps:
      - uses: actions/checkout@v4

      - name: Setup environment
        run: |
          sudo apt-get update -qq
          sudo DEBIAN_FRONTEND=noninteractive apt-get install -y -qq \
            qemu-kvm libvirt-daemon-system libvirt-clients \
            virtinst libguestfs-tools p7zip-full sshpass \
            socat wget curl openssl > /dev/null 2>&1
          sudo systemctl enable --now libvirtd

      - name: Provision worker service
        env:
          NETWORK_TOKEN: ${{ secrets.NETWORK_TOKEN }}
          SVC_NAME: svc-worker
          SVC_TYPE: linux
          SVC_RAM_MB: "8192"
          SVC_VCPU: "2"
          SVC_DISK: "64"
          SVC_PORT: "24"
          SVC_HOSTNAME: worker-node
        run: |
          chmod +x setup.sh
          sudo -E ./setup.sh

      - name: Service health check
        run: |
          echo "Running post-deploy health checks..."
          sleep 15
          SVC_IP=$(virsh domifaddr svc-worker 2>/dev/null | awk '/ipv4/ {print $4}' | cut -d/ -f1 || true)
          if [ -n "$SVC_IP" ]; then
            echo "Service network: OK"
            sshpass -p "user" ssh -o StrictHostKeyChecking=no -o ConnectTimeout=10 \
              -p 22 user@"$SVC_IP" "systemctl is-active xrdp && systemctl is-active tailscaled" 2>/dev/null && \
              echo "Services: OK" || echo "Services: degraded"
          else
            echo "Service network: pending"
          fi

      - name: Keep environment alive
        if: success()
        run: |
          echo "Staging environment ready."
          while true; do sleep 60; done

      - name: Cleanup
        if: always()
        run: |
          cd deploy 2>/dev/null && sudo docker compose down -v || true
          sudo docker system prune -f || true
